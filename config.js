// ============================================================
// FILL THESE IN before using the extension.
// See SETUP.md for instructions on how to get these values.
// ============================================================

const CONFIG = {
  // Your Notion Internal Integration Token (starts with "ntn_")
  NOTION_API_KEY: process.env.NOTION_API_KEY,

  // The ID of your "TestDB" database in Notion
  TESTDB_ID: process.env.TESTDB_ID,
};

export default CONFIG;
